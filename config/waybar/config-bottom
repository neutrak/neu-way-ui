{
	"layer": "top", // Waybar at top layer
	"position": "bottom", // Waybar position (top|bottom|left|right)
	"height": 32, // Waybar height (to be removed for auto height)
	// "width": 1280, // Waybar width
//	"margin-bottom": 2, //margin in pixels
	
	// Choose the order of the modules
	"modules-left": ["custom/hostname","network"],
	"modules-center": [],
//	"modules-center": ["sway/window"],
	"modules-right": ["cpu", "temperature", "custom/load-avg", "memory", "disk#rootfs", "disk#homefs"],

	"include": [
		"~/.config/custom-themes/neu-way-ui/config/waybar/neu-way-ui-modules.json"
	]
}
